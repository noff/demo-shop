<%= simple_form_for(@comment) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
	  <%= f.input :item_id, :as => :hidden %>
	  <%= f.input :message, input_html: {cols: 60, rows: 5} %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
